# 개발 일지 (3) — 파트별 세부 작업 계획

> 기준일: 2026-02-21
> 이전 문서: 개발 일지(2) — 전략 수정본
> 목적: 개발 일지(2)의 Part·Stage 전략을 세부 작업 단위로 분해하고, 개발 일지(2)에 없던 Part 4~6을 추가하여 전체 로드맵을 완성

---

## 전체 구조 요약

```
Part 1 — 개발 파이프라인       Stage 1~5   (토대·환경)
Part 2 — 시스템 파이프라인     Stage 1~6   (핵심 에이전트)
Part 3 — 모니터링 파이프라인   Stage 1~4   (가시성)
Part 4 — 지식·캐시 레이어      Stage 1~3   (성능·지식 공유)
Part 5 — 의사결정 레이어       Stage 1~2   (자율 판단·진화)
Part 6 — CEO 대시보드          Stage 1~4   (운영 UI)
```

각 Stage의 작업이 끝날 때마다 `00_COMMON_RULES.md` 9절 절차에 따라:
1. 전략 제시 + 질문 5개 → 답변 대기 → 구현
2. 해당 파트 개발일지 작성 후 다음 Stage 착수

---

## 작업 템플릿 (전체 공통)

모든 Stage는 아래 구조로 기술합니다.

```
선행 조건 | 세부 작업 목록 | 생성 파일 | 완료 기준(DoD)
```

---

## Part 1 — 개발 파이프라인

> **목표**: 이후 모든 작업의 토대. 개발 환경·코드 품질 체계·관측 기반 완성
> **관련 규칙**: `00_COMMON_RULES.md`, `06_MLOPS_RULES.md`

---

### Stage 1: 리포 골격 + 인터페이스 선 정의 + 관측 기반

**목적**: 모든 레이어 디렉토리를 생성하고 LLMProvider Protocol·JSON 로그·에러코드를 선 정의하여
이후 모든 Stage가 일관된 구조에서 시작할 수 있도록 한다.

**선행 조건**: 없음 (시작점)

**세부 작업**:

```
1.  프로젝트 루트 디렉토리 위치 확정 및 생성
2.  5개 레이어 디렉토리 + __init__.py 생성
    domain/ / application/ / adapters/ / interfaces/ / observability/
3.  observability/ids.py        — run_id / task_id / message_id 생성 함수 (UUID v4)
4.  observability/logger.py     — JSON 구조화 로그 설정 (structlog)
5.  observability/error_codes.py — ErrorCode Enum 전체 정의
6.  domain/ports.py             — LLMProvider / WorkSpacePort / StoragePort Protocol 선 정의
7.  adapters/mock_llm_provider.py — 테스트용 MockLLMProvider (응답 문자열 주입)
8.  requirements.txt            — pydantic, aiohttp, aiosqlite, structlog
9.  requirements-dev.txt        — pytest, pytest-asyncio, black, flake8, mypy, isort
10. .env.example                — 키만, 값 없음
```

**생성 파일**:

```
project-root/
├── domain/
│   ├── __init__.py
│   └── ports.py               ← LLMProvider / WorkSpacePort / StoragePort Protocol
├── application/
│   └── __init__.py
├── adapters/
│   ├── __init__.py
│   └── mock_llm_provider.py
├── interfaces/
│   └── __init__.py
├── observability/
│   ├── __init__.py
│   ├── ids.py
│   ├── logger.py
│   └── error_codes.py
├── requirements.txt
├── requirements-dev.txt
└── .env.example
```

**완료 기준 (DoD)**:

- [ ] 5개 레이어 디렉토리 생성 완료
- [ ] `from observability.error_codes import ErrorCode` 정상 import
- [ ] `from domain.ports import LLMProvider` 정상 import
- [ ] JSON 로그에 `run_id` 포함되어 출력됨을 콘솔에서 확인
- [ ] `domain/ports.py`에 aiohttp 등 외부 라이브러리 import 없음 (mypy 통과)

---

### Stage 2: 로컬 실행 환경 (Makefile, setup.sh)

**목적**: 모든 작업자(AI 포함)가 동일한 명령으로 환경을 구성하고 테스트를 실행할 수 있는
표준 진입점을 확립한다.

**선행 조건**: Stage 1 완료

**세부 작업**:

```
1. deploy/setup.sh    — Python 버전 확인·venv 생성·의존성 설치·.env 복사
2. Makefile           — setup / lint / format / test / test-fast / test-e2e 타겟
3. pyproject.toml     — black(line-length=100) / isort / pytest 설정 통합
4. .flake8            — max-line-length=100, ignore=E203,W503
5. mypy.ini           — strict=false, python_version=3.10
6. .gitignore         — venv/ / .env / __pycache__ / *.pyc / .pytest_cache / outputs/
```

**생성 파일**:

```
project-root/
├── deploy/
│   └── setup.sh
├── Makefile
├── pyproject.toml
├── .flake8
├── mypy.ini
└── .gitignore
```

**완료 기준 (DoD)**:

- [ ] `bash deploy/setup.sh` → 환경 완전 구성 (에러 없음)
- [ ] `make lint` → 통과 (소스 없어도 runner 자체 동작 확인)
- [ ] `make format` → 오류 없음
- [ ] `make test-fast` → "no tests collected" 아닌 정상 종료

---

### Stage 3: 테스트 체계 (단위 / 통합 / E2E 분리)

**목적**: 레이어별 테스트 디렉토리를 분리하고 MockLLMProvider로 첫 단위 테스트를 작성하여
테스트 파이프라인이 정상 동작함을 확인한다.

**선행 조건**: Stage 2 완료

**세부 작업**:

```
1. tests/unit/ / tests/integration/ / tests/e2e/ 디렉토리 + __init__.py 생성
2. tests/conftest.py        — 공통 픽스처 (MockLLMProvider, 임시 DB 경로)
3. tests/unit/conftest.py   — 단위 테스트 전용 픽스처
4. tests/unit/test_error_codes.py — ErrorCode Enum 전 항목 import 테스트
5. tests/unit/test_ids.py         — run_id / task_id / message_id 생성 포맷 검증
6. tests/unit/test_logger.py      — JSON 로그 출력 포맷 검증 (run_id 포함 여부)
7. pyproject.toml pytest 설정
   testpaths = tests/unit tests/integration
   markers: slow (E2E용)
```

**생성 파일**:

```
tests/
├── conftest.py
├── unit/
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_error_codes.py
│   ├── test_ids.py
│   └── test_logger.py
├── integration/
│   ├── __init__.py
│   └── conftest.py
└── e2e/
    ├── __init__.py
    └── conftest.py
```

**완료 기준 (DoD)**:

- [ ] `pytest --collect-only` 에러 없음
- [ ] `make test-fast` → 단위 테스트 전체 통과
- [ ] MockLLMProvider fixture를 사용한 단위 테스트 1개 이상 통과
- [ ] E2E 테스트는 `@pytest.mark.slow` 마킹, 기본 실행(`make test`)에서 제외

---

### Stage 4: CI 파이프라인

**목적**: main 브랜치 PR 시 lint → test → build가 자동으로 실행되어
코드 품질을 사람 없이 보장한다.

**선행 조건**: Stage 3 완료

**세부 작업**:

```
1. .github/workflows/ci.yml 작성
   - Job 1: lint       (black --check / flake8 / mypy / isort --check)
   - Job 2: unit-test  (tests/unit/ — MockLLMProvider)
   - Job 3: integration(tests/integration/)
   - Job 4: build      (패키지 빌드 검증)
2. GitHub Actions 버전 검증 (actions/checkout@v4, setup-python@v5)
3. pip 캐시 활성화 (cache: 'pip')
4. 커버리지 업로드 (codecov/codecov-action@v4)
5. E2E 테스트는 CI에 포함하지 않음 (Ollama 필요)
```

**생성 파일**:

```
.github/
└── workflows/
    └── ci.yml
```

**완료 기준 (DoD)**:

- [ ] CI yml 문법 검증 통과
- [ ] lint → unit-test → integration → build 순서 실행 확인
- [ ] E2E 테스트 CI 미포함 확인
- [ ] **Part 1 → Part 2 진입 Gate**: main 브랜치 첫 PR이 CI green으로 머지 완료

---

### Stage 5: 배포 스크립트 / 릴리스 규칙

**목적**: 로컬 실행을 자동화하고 Conventional Commits 기반 릴리스 절차를 문서화한다.

**선행 조건**: Stage 4 완료

**세부 작업**:

```
1. deploy/run.sh 작성
   - Ollama 설치 확인 → 없으면 [ERROR] 출력 후 exit 1
   - 필요 모델 확인 및 pull (llama3.1:70b / phi3.5 / llama3.2:3b)
   - DB 초기화 (idempotent)
   - python main.py 실행
2. main.py — 진입점 스켈레톤 (빈 async main 함수)
3. docs/CONTRIBUTING.md — Conventional Commits 규칙 문서화
```

**완료 기준 (DoD)**:

- [ ] `bash deploy/run.sh` → Ollama 없으면 [ERROR] 메시지 후 종료
- [ ] `set -euo pipefail` 첫 줄 적용
- [ ] 스크립트 2회 이상 실행해도 동일 결과 (멱등성 보장)

---

### Part 1 Gate

```
내부 기준: make test-fast 전 구간 green + CI yml 문법 검증 통과
진입 Gate: main 브랜치 첫 PR이 GitHub Actions CI green으로 머지 완료
```

---

## Part 2 — 시스템 파이프라인

> **목표**: CTO Agent → SLM Agent → Collaboration Layer 전체 흐름 구현
> **관련 규칙**: `01_ORCHESTRATION_RULES.md`, `02_AGENT_EXECUTION_RULES.md`,
>               `03_COLLABORATION_RULES.md`, `04_INFRASTRUCTURE_RULES.md`

---

### Stage 1: 도메인 계약 정의 (스키마 고정)

**목적**: 모든 에이전트가 공유하는 데이터 계약을 Pydantic 모델로 선 정의하고,
이후 변경 시 BREAKING CHANGE 절차를 의무화한다.

**선행 조건**: Part 1 Gate 통과

**세부 작업**:

```
1. domain/contracts/ 디렉토리 생성
2. domain/contracts/work_item.py  — WorkItem, WorkStatus(Enum)
   WorkStatus: PLANNED / WAITING / IN_PROGRESS / DONE / FAILED / BLOCKED
3. domain/contracts/message.py    — Message, MessageType, MessageStatus
4. domain/contracts/strategy.py   — Strategy, Task
5. domain/contracts/task_result.py — TaskResult, FileInfo
6. domain/contracts/agent_dna.py  — AgentDNA (Phase 5 대비 선 정의, 최소 필드)
7. domain/contracts/__init__.py   — 전체 re-export
8. 각 계약별 단위 테스트 작성 (직렬화·역직렬화 검증)
```

**생성 파일**:

```
domain/contracts/
├── __init__.py
├── work_item.py
├── message.py
├── strategy.py
├── task_result.py
└── agent_dna.py

tests/unit/
├── test_work_item.py
├── test_message.py
├── test_strategy.py
└── test_task_result.py
```

**완료 기준 (DoD)**:

- [ ] 전체 스키마 Pydantic BaseModel로 선언 완료
- [ ] `domain/contracts/` 외부에 스키마 정의 없음
- [ ] 각 스키마 단위 테스트 1개 이상 통과
- [ ] WorkStatus 허용 전이 경로가 Enum 또는 별도 검증 함수에 명시

---

### Stage 2: LLM Provider Adapter

**목적**: Ollama API를 호출하는 OllamaProvider를 LLMProvider Protocol 구현체로 작성하고
health_check → generate 흐름과 에러 처리를 완성한다.

**선행 조건**: Stage 1 완료

**세부 작업**:

```
1. adapters/ollama_config.py    — OllamaConfig (모델명·온도·타임아웃 설정)
2. adapters/ollama_provider.py  — OllamaProvider
   - __aenter__ / __aexit__ 세션 관리 (ClientSession 1개 유지)
   - health_check() — Ollama 서버 및 모델 존재 확인 → False 시 즉시 종료
   - generate()     — 재시도 3회, 타임아웃 300초
   - 에러: E_LLM_TIMEOUT / E_LLM_UNAVAILABLE / E_LLM_MODEL_MISSING
3. tests/unit/test_ollama_provider.py      — MockLLMProvider 단위 테스트
4. tests/integration/test_ollama_integration.py — 실제 Ollama 연동 (선택)
```

**생성 파일**:

```
adapters/
├── ollama_config.py
└── ollama_provider.py

tests/unit/
└── test_ollama_provider.py
```

**완료 기준 (DoD)**:

- [ ] MockLLMProvider 단위 테스트 통과
- [ ] health_check() False 시 즉시 종료 동작 확인
- [ ] 재시도 3회 / 타임아웃 300초 설정 확인
- [ ] 모든 에러에 run_id + ErrorCode 포함 로그 출력
- [ ] mypy: OllamaProvider가 LLMProvider Protocol을 완전히 구현

---

### Stage 3: CTO Orchestration Agent

**목적**: 전략 수립과 작업 분해를 담당하는 CTOAgent를 구현한다.
LLMProvider를 주입받아 Ollama 직접 참조 없이 동작한다.

**선행 조건**: Stage 2 완료

**세부 작업**:

```
1. application/cto_agent.py
   - create_strategy(project_request: str) -> Strategy
   - decompose_tasks(strategy: Strategy) -> list[Task]
   - review_progress(work_items: list[WorkItem]) -> str
2. application/prompts/cto_strategy.txt   — 전략 수립 프롬프트
3. application/prompts/cto_decompose.txt  — 작업 분해 프롬프트
4. JSON 파싱 실패 시 재시도 3회 → E_PARSE_JSON
5. tests/unit/test_cto_agent.py           — MockLLMProvider 사용
```

**생성 파일**:

```
application/
├── cto_agent.py
└── prompts/
    ├── cto_strategy.txt
    └── cto_decompose.txt

tests/unit/
└── test_cto_agent.py
```

**완료 기준 (DoD)**:

- [ ] `create_strategy()` → Strategy 객체 반환 (Mock 기반)
- [ ] `decompose_tasks()` → List[Task] 반환 (Mock 기반)
- [ ] JSON 파싱 실패 시 재시도 3회 후 E_PARSE_JSON 에러
- [ ] CTOAgent가 LLMProvider Protocol만 참조 (OllamaProvider 직접 import 없음)

---

### Stage 4: SLM Execution Agents

**목적**: Frontend / Backend / MLOps 역할별 SLM Agent를 구현한다.
각 Agent는 역할별 프롬프트를 파일로 분리해 사용한다.

**선행 조건**: Stage 3 완료

**세부 작업**:

```
1. application/base_agent.py    — BaseSLMAgent 추상 클래스
   - execute_task(task, workspace) -> TaskResult
   - 의존성 대기 루프: 2초 폴링 / 300초 타임아웃 / E_DEPS_TIMEOUT
2. application/frontend_agent.py — FrontendSLMAgent (phi3.5)
3. application/backend_agent.py  — BackendSLMAgent (phi3.5)
4. application/mlops_agent.py    — MLOpsSLMAgent (llama3.2:3b)
5. application/prompts/
   frontend.txt / backend.txt / mlops.txt
6. JSON 응답 파싱 → TaskResult 변환 (재시도 포함)
7. tests/unit/test_slm_agents.py
```

**생성 파일**:

```
application/
├── base_agent.py
├── frontend_agent.py
├── backend_agent.py
├── mlops_agent.py
└── prompts/
    ├── frontend.txt
    ├── backend.txt
    └── mlops.txt

tests/unit/
└── test_slm_agents.py
```

**완료 기준 (DoD)**:

- [ ] `execute_task()` → TaskResult 반환 (MockWorkspace + MockLLM)
- [ ] WAITING 상태 WorkItem 있으면 폴링 후 재개
- [ ] 타임아웃 초과 시 E_DEPS_TIMEOUT 에러
- [ ] 역할별 프롬프트 파일이 Python 코드와 완전히 분리
- [ ] 맛보기: "Todo 앱" Task 입력 → 코드 문자열 반환 확인

---

### Stage 5: SQLite Storage + Collaboration Layer

**목적**: WorkSpace / MessageQueue / EventBus를 구현하고 SQLite 영속화 레이어와 연결한다.

**선행 조건**: Stage 4 완료

**세부 작업**:

```
[SQLite Storage]
1. adapters/sqlite_storage.py
   - WAL 모드 / FOREIGN KEYS ON
   - 파라미터 바인딩 필수 (f-string SQL 금지)
   - 복수 쓰기는 트랜잭션으로 묶음
2. adapters/migrations/001_initial_schema.sql — 전체 테이블 DDL

[WorkSpace]
3. application/workspace.py — SharedWorkspace
   - 인메모리 캐시 + SQLite 동기화
   - WorkItem 상태 전이 검증 (금지 경로 → WorkspaceError)
   - 블로킹 감지 → BlockingDetectedEvent 발행

[MessageQueue]
4. application/message_queue.py
   - 키워드 기반 라우팅 (MVP)
   - Q&A 타임아웃 30초 → 빈 문자열 반환 (예외 아님)
   - 모든 메시지 DB 영구 저장

[EventBus]
5. application/event_bus.py
   - WorkItemUpdatedEvent / BlockingDetectedEvent 핸들러
   - 핸들러 10초 이내 완료 목표

6. tests/integration/test_collaboration.py
```

**생성 파일**:

```
adapters/
├── sqlite_storage.py
└── migrations/
    └── 001_initial_schema.sql

application/
├── workspace.py
├── message_queue.py
└── event_bus.py

tests/integration/
└── test_collaboration.py
```

**완료 기준 (DoD)**:

- [ ] WorkItem 상태 전이 전 구간 단위 테스트 통과
- [ ] 금지 전이(DONE→IN_PROGRESS 등) 시 WorkspaceError 발생
- [ ] "api" 키워드 → backend 에이전트 라우팅 확인
- [ ] Q&A 타임아웃 → 빈 문자열 반환, 예외 없음
- [ ] 블로킹 감지 → BlockingDetectedEvent 발행 확인

---

### Stage 6: 파일 저장 + E2E 시나리오 검증

**목적**: TaskResult를 실제 파일로 저장하고 3개 시나리오로 전체 흐름을 검증한다.

**선행 조건**: Stage 5 완료

**세부 작업**:

```
1. adapters/file_storage.py
   - save_result_files() — pathlib.Path / utf-8 인코딩 명시
   - outputs/{project_name}-{run_id}/ 구조 생성
   - README.md 자동 생성 (project_name / run_id / 생성 시각 / 기술 스택)
2. main.py 완성 — 전체 파이프라인 진입점 (async main)
3. E2E 시나리오 (@pytest.mark.slow):
   - Todo 앱  — 기본 협업 검증
   - 블로그 앱 — 의존성·Q&A 검증
   - 방명록 앱 — 전체 안정성 검증
```

**생성 파일**:

```
adapters/
└── file_storage.py

main.py

tests/e2e/
├── test_todo_app.py
├── test_blog_app.py
└── test_guestbook_app.py

outputs/          ← .gitignore에 포함
```

**완료 기준 (DoD)**:

- [ ] 3개 프로젝트 완성도 60% 이상 (코드 파일 실제 생성)
- [ ] `outputs/{project}-{run_id}/` 구조로 파일 저장 확인
- [ ] README.md 자동 생성 확인
- [ ] 실패 지점을 trace_id + task_id로 30초 내 식별 가능

---

### Part 2 Gate

```
"모든 실패 지점이 로그에서 trace_id + stage_id + task_id + ErrorCode로
 30초 내에 식별 가능한 상태"
```

---

## Part 3 — 모니터링 파이프라인

> **목표**: 시스템 운영 가시성 확보. 메트릭 수집 → 트레이싱 → 대시보드 → 런북
> **관련 규칙**: `04_INFRASTRUCTURE_RULES.md`
> **참고**: JSON 로그·ID 체계·에러코드는 Part 1 Stage 1에서 완료. Stage 1부터 시작.

---

### Stage 1: 핵심 메트릭 수집

**목적**: 작업 성공률·LLM 호출 수 등 핵심 지표를 수집하여 시스템 상태를 수치로 파악한다.

**선행 조건**: Part 2 Gate 통과

**세부 작업**:

```
1. observability/metrics.py — MetricsCollector
   수집 대상:
   - 작업 성공률 / 평균 소요 시간 / 재시도 횟수
   - LLM 호출 수 / 토큰 수 / 에러율
   - 협업 Q&A 성공률 / 타임아웃 빈도
2. adapters/migrations/002_add_metrics.sql — system_metrics 테이블 추가
3. E2E 실행 후 SQLite에 메트릭 저장 검증
```

**완료 기준 (DoD)**:

- [ ] E2E 실행 후 SQLite system_metrics 테이블에 데이터 저장 확인
- [ ] 성공률·평균 소요 시간 조회 쿼리 정상 동작

---

### Stage 2: 트레이싱 (요청 → CTO → SLM → Workspace 흐름)

**목적**: 요청 하나의 전체 흐름을 trace_id로 추적하여 병목·실패 지점을 빠르게 식별한다.

**선행 조건**: Stage 1 완료

**세부 작업**:

```
1. observability/tracing.py — trace_id 기반 흐름 추적
2. 각 Agent 전환 시점에 trace 로그 삽입
3. trace_id가 로그 전 구간에 전파되는지 단위 테스트 검증
```

**완료 기준 (DoD)**:

- [ ] 요청 단위 전체 흐름을 trace_id로 추적 가능
- [ ] Agent 간 전환 시점 로그에 trace_id 명시

---

### Stage 3: CLI 대시보드

**목적**: 터미널에서 시스템 상태를 실시간으로 확인하는 간단한 대시보드를 제공한다.

**선행 조건**: Stage 2 완료

**세부 작업**:

```
1. interfaces/cli_dashboard.py — rich 기반 CLI 대시보드
   표시 항목:
   - 현재 실행 중인 WorkItem 목록 및 상태
   - 에이전트별 성공률·평균 소요 시간
   - 최근 에러 목록 (ErrorCode 기준)
2. 1초 폴링으로 실시간 갱신
3. requirements.txt에 rich 추가
```

**완료 기준 (DoD)**:

- [ ] `python -m interfaces.cli_dashboard` 실행 → 터미널 대시보드 출력
- [ ] 1초 주기 갱신 확인

---

### Stage 4: 장애 대응 런북

**목적**: 로그만 보고 장애 원인을 파악하고 복구할 수 있는 절차를 문서화한다.

**선행 조건**: Stage 3 완료

**세부 작업**:

```
1. docs/RUNBOOK.md 작성
   - ErrorCode 전체 항목별 대응 절차
   - Stage별 "되돌릴 지점" 1개씩 고정
   - 장애 재현 없이 로그만으로 원인 파악하는 절차
```

**완료 기준 (DoD)**:

- [ ] ErrorCode Enum 전 항목에 대응 절차 기재
- [ ] Stage마다 "되돌릴 지점" 1개 명시

---

### Part 3 Gate

```
"장애 재현 없이 로그만 보고 원인 파악 가능"
```

---

## Part 4 — 지식·캐시 레이어

> **목표**: Redis 캐시로 응답 속도 향상, Qdrant 벡터 DB로 에이전트 간 지식 공유 실현
> **관련 규칙**: `04_INFRASTRUCTURE_RULES.md` (Redis 섹션)

---

### Stage 1: Redis 캐시 레이어

**목적**: WorkItem 상태를 Redis에 캐싱하여 SQLite 조회 빈도를 줄이고 응답 속도를 개선한다.

**선행 조건**: Part 3 Gate 통과

**세부 작업**:

```
1. adapters/redis_cache.py — RedisCache
   - 키 네이밍: work_item:{id} / agent_status:{id} / run_metrics:{id}
   - 모든 키에 TTL 필수 (최소 3600초)
2. SharedWorkspace → Redis 캐시 우선 조회로 전환
   (Redis 미연결 시 SQLite 폴백)
3. .env.example에 REDIS_URL 추가
```

**완료 기준 (DoD)**:

- [ ] 모든 Redis 키에 TTL 설정 확인
- [ ] Redis 미연결 시 SQLite 폴백 동작 확인

---

### Stage 2: Qdrant 벡터 DB + KnowledgeGraph

**목적**: 에이전트 간 Q&A 이력을 벡터화하여 유사 질문에 자동으로 답변을 재사용한다.

**선행 조건**: Stage 1 완료

**세부 작업**:

```
1. adapters/qdrant_storage.py — QdrantAdapter
2. application/knowledge_graph.py — KnowledgeGraph
   - Q&A 이력 벡터화 저장
   - 유사도 검색 기반 답변 재사용
3. domain/ports.py에 KnowledgeGraphPort 추가
4. requirements.txt에 qdrant-client 추가
```

**완료 기준 (DoD)**:

- [ ] Q&A 이력이 Qdrant에 저장되고 유사 질문에서 검색 가능
- [ ] 검색 결과가 MessageQueue 응답에 활용됨

---

### Stage 3: KnowledgeGraph 기반 메시지 라우팅

**목적**: MessageQueue의 키워드 라우팅을 KnowledgeGraph 기반으로 교체하여
더 정확한 전문가 에이전트에게 질문을 전달한다.

**선행 조건**: Stage 2 완료

**세부 작업**:

```
1. MessageQueue 라우팅 로직 교체
   기존: 키워드 매칭 → 고정 에이전트
   변경: KnowledgeGraph.expertise_level 최고값 에이전트로 라우팅
2. KnowledgeGraph 라우팅 실패 시 키워드 라우팅 폴백
3. 라우팅 결정 로그에 선택 이유 포함
```

**완료 기준 (DoD)**:

- [ ] KnowledgeGraph 라우팅 우선, 키워드 폴백 동작 확인
- [ ] 라우팅 결정 로그에 선택 이유 포함

---

### Part 4 Gate

```
"KnowledgeGraph 라우팅 성공률 ≥ 키워드 라우팅 성공률"
```

---

## Part 5 — 의사결정 레이어

> **목표**: Stage Gate 회의 자동화 + AgentDNA 진화 시스템으로 에이전트가 스스로 개선
> **관련 규칙**: `01_ORCHESTRATION_RULES.md`, `03_COLLABORATION_RULES.md`

---

### Stage 1: Stage Gate Meeting 시스템

**목적**: Part 전환 시 자동으로 Gate 회의를 소집하고 진행 여부를 CTO Agent가 결정한다.

**선행 조건**: Part 4 Gate 통과

**세부 작업**:

```
1. application/stage_gate.py — StageGateMeeting
   - Gate 조건 자동 평가
   - CTO Agent가 진행/중단 결정
   - 회의 결과 DB 저장
2. adapters/migrations/003_stage_gate.sql
3. BlockingDetectedEvent 구독 → 긴급 회의 소집 트리거
4. tests/unit/test_stage_gate.py
```

**완료 기준 (DoD)**:

- [ ] Part 전환 시 Gate 조건 자동 평가 동작
- [ ] 블로킹 감지 → 긴급 회의 소집 확인
- [ ] 회의 결과 DB 저장 확인

---

### Stage 2: AgentDNA 진화 시스템

**목적**: 작업 결과를 기반으로 각 에이전트의 전문성 수치(DNA)를 자동 업데이트하여
더 적합한 에이전트에게 작업을 배분한다.

**선행 조건**: Stage 1 완료

**세부 작업**:

```
1. application/dna_manager.py — DNAManager
   - TaskResult 기반 DNA 수치 업데이트 (성공률 / 소요 시간)
   - 전문성 분야별 가중치 계산
2. adapters/migrations/004_agent_dna.sql
3. 작업 완료 이벤트 구독 → DNA 자동 갱신
4. DNA 기반 작업 배분 우선순위 적용
```

**완료 기준 (DoD)**:

- [ ] 작업 완료 시 AgentDNA 자동 업데이트 확인
- [ ] DNA 수치 기반 작업 배분 우선순위 적용 확인

---

### Part 5 Gate

```
"3회 이상 E2E 실행 시 에이전트 DNA 수치가 변화함을 확인"
```

---

## Part 6 — CEO 대시보드

> **목표**: Flutter Web으로 시스템 운영 상황을 실시간으로 모니터링하는 UI 제공
> **관련 규칙**: `05_FRONTEND_RULES.md`

---

### Stage 1: Flutter Web 기본 구조 + FastAPI WebSocket 서버

**목적**: CEO 대시보드 Flutter 프로젝트를 생성하고 백엔드 WebSocket 서버와 연결한다.

**선행 조건**: Part 5 Gate 통과

**세부 작업**:

```
[백엔드]
1. interfaces/websocket_server.py — FastAPI WebSocket 엔드포인트
2. requirements.txt에 fastapi, uvicorn 추가

[Flutter]
3. ui/ceo_dashboard/ — Flutter Web 프로젝트 생성
4. Riverpod 상태 관리 설정
5. WebSocket 연결 + 자동 재연결 구현 (끊김 → 3초 대기 → 재연결)
6. 라우팅 설정: / / /agents / /projects / /logs / /metrics
7. 테마 및 공통 상수 설정 (core/constants.dart / core/theme.dart)
```

**생성 파일**:

```
interfaces/
└── websocket_server.py

ui/ceo_dashboard/
├── lib/
│   ├── core/
│   │   ├── constants.dart
│   │   ├── router.dart
│   │   └── theme.dart
│   ├── data/
│   │   ├── models/
│   │   └── repositories/
│   ├── presentation/
│   │   ├── pages/
│   │   └── widgets/
│   └── providers/
└── test/
```

**완료 기준 (DoD)**:

- [ ] Flutter Web 로컬 빌드 성공 (`flutter build web`)
- [ ] WebSocket 연결 끊김 → 3초 후 자동 재연결 동작
- [ ] WorkItem 상태 변경 시 UI 즉시 반영

---

### Stage 2: 대시보드 페이지 5개 구현

**목적**: 시스템 운영에 필요한 5개 필수 페이지를 구현한다.

**선행 조건**: Stage 1 완료

**세부 작업**:

```
1. / (홈)       — 현재 실행 중인 프로젝트 요약 카드
2. /agents      — 에이전트별 상태 + DNA 수치 시각화 (fl_chart)
3. /projects    — 프로젝트 목록 및 완성도 진행바
4. /logs        — 실시간 로그 스트림 (무한 스크롤)
5. /metrics     — 성공률 / 평균 시간 / LLM 토큰 수 차트

공통 적용:
- 로딩 상태: Spinner 위젯
- 에러 상태: ErrorMessage 위젯
- 768px 이상 반응형
- aria-label 기본 접근성
```

**완료 기준 (DoD)**:

- [ ] 5개 페이지 모두 768px 이상에서 정상 렌더링
- [ ] 모든 비동기 컴포넌트에 로딩·에러 상태 처리 적용

---

### Stage 3: Widget 테스트 + 백엔드 통합 테스트

**목적**: Flutter 위젯 단위 테스트와 실제 백엔드 연동 E2E 테스트를 완성한다.

**선행 조건**: Stage 2 완료

**세부 작업**:

```
1. test/widget/ — 각 페이지 Widget 테스트
2. test/unit/   — Provider 단위 테스트
3. 백엔드 없이도 에러 상태 정상 표시 확인
4. 실제 백엔드와 연동 E2E 테스트 1개 이상
```

**완료 기준 (DoD)**:

- [ ] Widget 테스트 전 구간 통과
- [ ] 백엔드 없이도 에러 상태 정상 표시

---

### Part 6 Gate (최종)

```
"CEO 대시보드에서 실시간으로 에이전트 상태·작업 진행률 확인 가능"
```

---

## 전체 진행 순서

```
Part 1 (개발 파이프라인)      Stage 1 → 2 → 3 → 4 → 5 → Gate
      │
      ▼
Part 2 (시스템 파이프라인)    Stage 1 → 2 → 3 → 4 → 5 → 6 → Gate
      │
      ▼
Part 3 (모니터링)             Stage 1 → 2 → 3 → 4 → Gate
      │
      ▼
Part 4 (지식·캐시)            Stage 1 → 2 → 3 → Gate
      │
      ▼
Part 5 (의사결정)             Stage 1 → 2 → Gate
      │
      ▼
Part 6 (CEO 대시보드)         Stage 1 → 2 → 3 → Gate (최종)
```

---

## 이슈 기록란

| 이슈 | 차단성 | 전파성 | 발현 시점 | 수정 비용 | 최종 | 발견 Stage |
|------|-------|-------|---------|---------|------|-----------|
| (작업 진행 중 기록) | | | | | | |

---

*다음 개발 일지: 각 파트별 개발일지 폴더에 Stage 완료 시마다 작성*
*예: `개발 일지/Part1 개발일지/개발 일지(1).md` — Part 1 Stage 1 완료 시*
