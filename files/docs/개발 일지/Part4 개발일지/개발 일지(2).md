# Part 4 개발 일지 (2) — MLOps Stage 2: docker-compose.yml

## 날짜
2026-04-21

## 작업 목표
MLOps Stage 2 구현 — docker-compose.yml (헬스체크, 환경변수 참조, restart 정책 포함)

## 구현 내용

### Stage 2 검증 조건
| 검증 항목 | 매칭 방법 |
|---|---|
| docker-compose.yml 존재 | path에 "docker-compose" + ".yml"/".yaml" |
| services: 섹션 | `"services:"` in compose_content |
| healthcheck: | `"healthcheck:"` in compose_content |
| 환경변수 참조 (${ }) | `"${"` in compose_content |
| restart 정책 | `"restart:"` in compose_content |

### 환경변수 검증 설계 의도
`${VAR}` 형식 검증 — 하드코딩된 값(예: `DATABASE_URL=postgres://localhost/db`) 대신 실제 환경변수 참조를 강제함으로써 보안 모범 사례 준수.

## 테스트 결과
| 테스트 | 결과 |
|---|---|
| test_mlops_stage2_success | PASS |
| test_mlops_stage2_selects_stage2_prompt | PASS |
| test_mlops_stage2_retry_when_healthcheck_missing_then_success | PASS |
| test_mlops_stage2_retry_when_env_var_ref_missing_then_success | PASS |
| test_mlops_stage2_fails_after_exhausting_retries | PASS |

**총 5개 전체 통과**

## 누적 검증 확인
Stage 2 agent는 `_stage >= 1`과 `_stage >= 2` 조건을 모두 실행하므로, Stage 1 요건(Dockerfile, .dockerignore)도 동시에 검증됨. Stage 2 성공 테스트의 픽스처(`_stage2_response`)는 Stage 1 파일들(`_stage1_base_files()`)을 기반으로 구성.

## 프롬프트 파일
`src/application/prompts/mlops/stage2_compose.txt` — healthcheck, ${VAR} 환경변수 참조 패턴, restart 정책 명시
