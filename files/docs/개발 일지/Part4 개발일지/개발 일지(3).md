# Part 4 개발 일지 (3) — MLOps Stage 3: CI/CD 파이프라인 + Makefile

## 날짜
2026-04-21

## 작업 목표
MLOps Stage 3 구현 — GitHub Actions 또는 GitLab CI + Makefile

## 설계 결정: GitHub Actions AND GitLab CI 모두 허용

### 이유
- 프로젝트마다 사용하는 CI 플랫폼이 다름
- 둘 중 하나를 강제하면 GitLab 기반 팀에서 Stage 3가 무조건 실패
- 검증 로직에 OR 조건 적용: `has_ci_pipeline = has_github_actions or has_gitlab_ci`

### Stage 3 검증 조건
| 검증 항목 | 매칭 방법 |
|---|---|
| CI 파이프라인 존재 | `.github/workflows/*.yml` OR `.gitlab-ci.yml` |
| lint job/stage | `"lint"` in ci_content (lower) |
| test job/stage | `"test"` in ci_content (lower) |
| pip cache 설정 | `"cache"` in ci_content (lower) |
| Makefile 존재 | path == "Makefile" |

## 테스트 결과
| 테스트 | 결과 |
|---|---|
| test_mlops_stage3_success_with_github_actions | PASS |
| test_mlops_stage3_success_with_gitlab_ci | PASS |
| test_mlops_stage3_selects_stage3_prompt | PASS |
| test_mlops_stage3_retry_when_makefile_missing_then_success | PASS |
| test_mlops_stage3_retry_when_ci_pipeline_missing_then_success | PASS |
| test_mlops_stage3_fails_after_exhausting_retries | PASS |

**총 6개 전체 통과**

## GitLab CI content 추출 로직
```python
ci_content = " ".join(
    c for p, c in entries
    if (
        ".github/workflows/" in p
        or p.endswith(".gitlab-ci.yml")
        or p.endswith("/.gitlab-ci.yml")
    )
)
```
두 CI 시스템의 콘텐츠를 하나의 문자열로 합쳐 lint/test/cache 키워드를 통합 검사.

## 프롬프트 파일
`src/application/prompts/mlops/stage3_cicd.txt` — lint/test 분리 job, pip cache, Makefile targets 명시
