# Part 4 개발 일지 (4) — MLOps Stage 4: 모니터링 + 배포 스크립트 (Part 4 Gate)

## 날짜
2026-04-21

## 작업 목표
MLOps Stage 4 구현 — Prometheus 설정, .env.example, 멱등성 배포 스크립트

## 모니터링 전략: B안 채택
- **A안** (Grafana 대시보드): 런타임 설치 복잡, JSON 파일 검증 난이도 높음
- **B안** (Prometheus + .env.example + deploy script): 정적 파일 기반, 키워드 검증 명확

## Stage 4 검증 조건
| 검증 항목 | 매칭 방법 |
|---|---|
| prometheus.yml 존재 | path에 "prometheus" + ".yml"/".yaml" |
| scrape_configs 섹션 | `"scrape_configs"` in prometheus_content |
| .env.example 존재 | path == ".env.example" |
| 배포 스크립트 존재 | `.sh` 확장자 + ("deploy"/"run"/"setup" in path) |
| set -euo pipefail | `"set -euo pipefail"` OR `"set -e"` in script |
| [INFO]/[ERROR] 로깅 | `"[info]"` OR `"[error]"` in script (lower) |

## 테스트 결과
| 테스트 | 결과 |
|---|---|
| test_mlops_stage4_success | PASS |
| test_mlops_stage4_selects_stage4_prompt | PASS |
| test_mlops_stage4_retry_when_prometheus_missing_then_success | PASS |
| test_mlops_stage4_retry_when_deploy_script_missing_then_success | PASS |
| test_mlops_stage4_retry_when_set_e_missing_then_success | PASS |
| test_mlops_stage4_fails_after_exhausting_retries | PASS |
| test_mlops_stage4_regression_stage1_still_passes | PASS |

**총 7개 전체 통과**

## Part 4 Gate 결과

### 전체 테스트 스위트
```
128 passed in 0.15s
```

### mypy 타입 검사
```
Success: no issues found in 61 source files
```

## 완성된 MLOps Agent 구조
```
src/application/
├── mlops_agent.py          ← MLOpsSLMAgent + MLOpsSLMConfig
└── prompts/mlops/
    ├── stage1_dockerfile.txt
    ├── stage2_compose.txt
    ├── stage3_cicd.txt
    └── stage4_monitoring.txt

tests/unit/
├── test_mlops_stage1.py    (7 tests)
├── test_mlops_stage2.py    (5 tests)
├── test_mlops_stage3.py    (6 tests)
└── test_mlops_stage4.py    (7 tests)
```

## 회고
- 누적 검증 설계로 Stage 4는 전체 인프라 스택(Dockerfile → Compose → CI → Monitoring)을 한번에 검증
- `_validate_mlops_extra_fields`의 공통 분리로 각 stage validator가 핵심 검증에 집중
- 회귀 테스트(`test_mlops_stage4_regression_stage1_still_passes`)로 Stage 4 추가 후에도 Stage 1이 독립적으로 동작함을 확인
